<h4>
  {{#link-to 'pedal' pedal}}
    <span class='card'>{{pedal.name}}</span>
  {{/link-to}}
  <button class='btn btn-primary btn-xs' {{action 'editPedal'}}>Edit</button>
  <button class='btn btn-default btn-xs btn-danger' {{action 'deletePedal'}}>Delete</button>
</h4>
