<h4>
  {{#link-to 'board' board}}
    {{board.name}}
  {{/link-to}}
  <button class='btn btn-primary btn-xs' {{action 'editBoard'}}>Edit</button>
  <button class='btn btn-default btn-xs btn-danger' {{action 'deleteBoard'}}>Delete</button>
</h4>
